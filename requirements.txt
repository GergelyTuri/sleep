# Pip-only packages not available via conda. Install after: pip install -r requirements.txt
pytest>=7.0
# gspread and google-auth: Colab-only. Install manually if using Google Drive
# integration (src/colab/google_drive.py). Not required for local analysis.
dabest~=0.3.1
